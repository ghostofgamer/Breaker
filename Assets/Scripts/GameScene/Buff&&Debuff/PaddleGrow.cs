using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PaddleGrow : Buff
{
    // private int _coefficien = 3;

    /*public override void ApplyEffect(GameObject target)
    {
        target.transform.localScale = new Vector3(target.transform.localScale.x + _coefficien,
            target.transform.localScale.y + _coefficien, target.transform.localScale.z + _coefficien);
    }*/
}