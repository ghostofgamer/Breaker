using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Wall : MonoBehaviour
{
    /*void OnTriggerExit(Collider other)
    {
        // Обрабатываем выход объекта за пределы области
        if (other.CompareTag("Ball"))
        {
            // Отражаем шар обратно в пределы области
            ReflectOffBoundary(other.gameObject);
        }
    }

    void ReflectOffBoundary(GameObject ball)
    {
        // Отражаем шар от границы (можно изменить направление на ваш выбор)
        ball.GetComponent<TestBallMover>().ReflectOffBoundary();
    }*/
}
